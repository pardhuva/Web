1. What is "conditional rendering"?
   when we want to only sometimes display something on the page based on some kind of condition 


2. When would you use &&?
   when you want to either display somehting or NOT display something 


3. When would you use a ternary?
   when you need to decide which of 2 things to displlay 


4. What if you need to decide between > 2 options on
   what to display?
   if......else if ......else consitional or may be 'switch' statement 
   